'use strict';

/**
 * translation-group service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::translation-group.translation-group');
