'use strict';

/**
 *  translation-group controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::translation-group.translation-group');
